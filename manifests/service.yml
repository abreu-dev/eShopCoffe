apiVersion: v1
kind: Service
metadata:
    name: "eshopcoffe-a7b0"
    labels:
        app: "eshopcoffe-a7b0"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "eshopcoffe-a7b0"